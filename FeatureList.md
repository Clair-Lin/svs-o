| **序号** | **功能**       | **描述**                             | **优先级** | 是否界面显示 | **备注** |
| -------- | -------------- | ------------------------------------ | ---------- | ------------ | -------- |
| 1        | 0019界面       | 0019创建密钥的界面                   | P0         | T            |          |
| 2        | 监控功能       | 系统状态、资源、SNMP监控**统一**展示 | P0         | T            |          |
| 3        | 白名单配置     | IP白名单访问控制                     | P0         | T            |          |
| 8        | 一键检测       | 服务接口检测、加密卡检测             | P1         | T            |          |
| 9        | 证书管理       | 按DN/有效期搜索、黑名单管理          | P1         | T            |          |
| 10       | zip证书导入    | 支持zip格式导入                      | P1         | T            |          |
| 4        | XML签名验签    | 支持封皮、封内、分离签名验签         | P0         | F            |          |
| 5        | PDF签名验签    | PDF签名、验签、图章签名、多重签名    | P1         | F            |          |
| 6        | 大文件签名验签 | 支持2GB以上大文件*1                  | P0         | F            |          |
| 7        | 加解密功能     | 对称/非对称加解密                    | P0         | F            |          |
| 11       | 部署模式       | 双机热备、负载均衡                   | P1         | F            |          |
| 12       | 818Key适配     | -                                    | P0         | F            |          |

### 1 监控功能

​                ● **系统状态**：CPU使用率（已有）、内存使用率（已有）、硬盘使用率（已有）、连接数

​                ● **业务统计**：管理员访问次数、证书数量、用户数量、应用实体数量、并发连接数、签名/验签业务、制作信封/解信封业务

​                ● **网络监控**：网络接口状态（已有）

### 2 访问控制

​                ● **IP白名单**：仅允许白名单内IP访问

​                ● **服务白名单**：应用服务器授权认证

### 3 部署与运维

​                ● **部署模式**：双机热备、负载均衡

​                ● **一键检测**：服务接口检测（获取服务器证书、签名、验签接口）、加密卡检测



### 4 白名单访问控制

​                ● **IP白名单**：，支持设置访问IP白名单，仅允许白名单内IP访问签名验签服务器

​                ○ 白名单配置：可对访问权限做控制，通过对连接白名单的支持，实现了密码机对应用服务器的授权认证

​                ○ 支持服务白名单功能，实现白名单的添加查看删除, 通过连接白名单的支持。